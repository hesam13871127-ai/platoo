name: vibetable
description: VibeTable social tabletop games
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.3.3
  intl: ^0.20.3
  go_router: ^14.6.2
  socket_io_client: ^2.0.3+1
  livekit_client: ^2.3.0
  google_sign_in: ^6.2.2
  sign_in_with_apple: ^6.1.4
  uuid: ^4.5.1
  cached_network_image: ^3.4.1
  # Production IAP: uncomment when wiring the real stores (see lib/features/iap/store_billing.dart).
  # in_app_purchase: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
